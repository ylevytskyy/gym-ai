services:
  api:
    build:
      context: .
    env_file:
      - .env
    ports:
      - "3000:3000"
    init: true
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "0.75"
          memory: 384M
        reservations:
          memory: 128M
