// CJS-compatible stub for the ESM-only `jose` package, used by jest via
// moduleNameMapper. Tests assign behavior via mockReset / mockResolvedValue.
export const jwtVerify = jest.fn();
export const createRemoteJWKSet = jest.fn();
